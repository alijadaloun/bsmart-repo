---
import { getAsset } from '~/utils/permalinks';
---

<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />

<link rel="sitemap" href={getAsset('/sitemap-index.xml')} />

<!-- Preload hero background images to prevent progressive "tile/stripe" loading -->
<link rel="preload" as="image" href={getAsset('/wave.webp')} type="image/webp" fetchpriority="high" />
<link rel="preload" as="image" href={getAsset('/wave_light_mode.webp')} type="image/webp" fetchpriority="high" />